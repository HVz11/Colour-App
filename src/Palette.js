import React, { Component } from "react";

class Palette extends Component {
  render() {
    return (
      <div className="Palette">
        {/* Navbar  */}
        <div className="Palette-colors">{/* bunch of colour boxes */}</div>
        {/* footer */}
      </div>
    );
  }
}

export default Palette;
